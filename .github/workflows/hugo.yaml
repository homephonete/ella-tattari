- name: Debug paths
  run: |
    echo "Repo root:"
    pwd
    echo "Files:"
    find . -maxdepth 3 -type f | sort

- name: Build with Hugo
  working-directory: ./hugo-portfolio
  run: |
    pwd
    ls -la
    hugo --gc --minify
